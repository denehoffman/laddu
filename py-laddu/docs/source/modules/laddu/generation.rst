generation
==========

Generation support is being moved into the ``laddu`` Python package. The
public Python module is not exposed yet, so this page is reserved for the
generated API reference once the package module lands.
