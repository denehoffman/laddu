optimize
========

Optimization and sampling helpers are currently exposed from
``laddu.extensions`` in Python. See :doc:`likelihood` for the generated
API reference until the Python modules are split into importable submodules.
